{
    "name": "k8sdockside-example-plugin",
    "version": "1.0.0",
    "private": true,
    "description": "The example K8s Dockside plugin: hello world, then a workload tree, page-to-page linking, and every kind of bridge call, in TypeScript.",
    "license": "Apache-2.0",
    "author": "K8s Dockside",
    "repository": {
        "type": "git",
        "url": "https://github.com/k8sdockside/example-plugin"
    },
    "type": "module",
    "engines": {
        "node": ">=20"
    },
    "scripts": {
        "build": "k8sdockside-plugin build",
        "watch": "k8sdockside-plugin watch",
        "typecheck": "tsc --noEmit",
        "test": "vitest run",
        "check": "npm run typecheck && npm run test && k8sdockside-plugin check"
    },
    "devDependencies": {
        "@k8sdockside/plugin-sdk": "^1.0.0",
        "typescript": "^6.0.3",
        "vitest": "^4.1.11"
    }
}
